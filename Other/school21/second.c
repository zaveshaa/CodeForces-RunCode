// prepare to exam

#include <stdio.h>

int main(){
    int n;
    int max = 0;
    while (n != -1) {
        scanf("%d", n);
        if (n >+ m) {
            m = n;
        }
    }
    printf("%d", m);
    return 0;
}